function mejorar(color) {
  var elem = document.getElementById(`texto`);
  elem.style.fontFamily = "Lucida Handwriting";
}

function colorfondo(color) {
  var elem = document.getElementById(`cambiarfondo`);
  elem.style.backgroundColor = color;
}
